defaultNamespace: secrets-system

helm:
  repo: https://bitnami-labs.github.io/sealed-secrets
  chart: sealed-secrets
  version: "2.7.0"
  releaseName: sealed-secrets-controller
  valuesFiles:
    - values.yaml

targetCustomizations: []
