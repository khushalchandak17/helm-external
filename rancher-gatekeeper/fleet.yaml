defaultNamespace: cattle-gatekeeper-system

helm:
  repo: https://charts.rancher.io
  chart: rancher-gatekeeper
  version: 101.0.0+up3.9.0
  releaseName: rancher-gatekeeper
  values:
    enableDeleteOperations: true
    logDenies: true
    constraintViolationsLimit: 500
    psp:
      enabled: false
    upgradeCRDs:
      enabled: false
    # Disable Validation Webhook by default
    disableValidatingWebhook: true
    validatingWebhookFailurePolicy: Ignore

# Enable Validation Webhook on select clusters
#targetCustomizations:
  # On-Prem QA
  #  - name: op-orw-sc-qa01
  #    clusterSelector:
  #      matchLabels:
  #       management.cattle.io/cluster-display-name: op-orw-sc-qa01
  #    helm:
  #      values:
  #        disableValidatingWebhook: true
