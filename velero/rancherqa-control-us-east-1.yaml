#
# AWS rancher-qa us-east-1 (local)
#
configuration:
  features: "EnableAPIGroupVersions"
  extraEnvVars:
    AWS_CLUSTER_NAME: rancher-qa
  backupStorageLocation:
    bucket: account-233164232659-us-east-1-kaas-velerobackups
    prefix: rancher-qa
    config:
      region: us-east-1
  volumeSnapshotLocation:
    config:
      region: us-east-1
schedules:
  hourly:
    disabled: false
  daily:
    disabled: false
