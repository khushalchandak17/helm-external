configuration:
  extraEnvVars:
    AWS_CLUSTER_NAME: op-orw-ovp-wargame03
  backupStorageLocation:
    bucket: orw-kaas-qa
    prefix: op-orw-ovp-wargame03
    config:
      # region: set to default us-east-1 because it's mandatory but will be ignored
      region: us-east-1
      s3ForcePathStyle: true
      s3Url: https://orw-ntap5-s3.wv.mentorg.com
credentials:
  useSecret: true
  existingSecret: orw-ntap5-s3-wv
schedules:
  hourly:
    disabled: false
  daily:
    disabled: false
  snapshots:
    disabled: false
