defaultNamespace: velero

helm:
  repo: https://vmware-tanzu.github.io/helm-charts
  chart: velero
  version: "2.27.3"
  releaseName: velero
  valuesFiles:
    - values.yaml

targetCustomizations: []
