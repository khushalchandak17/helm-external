#
# AWS rancherqa-wv  WilsonVille (on-prem vSphere) (local)
#
configuration:
  features: "EnableAPIGroupVersions"
  extraEnvVars:
    AWS_CLUSTER_NAME: rancherqa-wv
  backupStorageLocation:
    bucket: __TODO_QA_BUCKET__
    prefix: rancherqa-wv
    config:
      region: __TODO_REGION__
  volumeSnapshotLocation:
    config:
      region: __TODO_REGION__
schedules:
  hourly:
    disabled: false
  daily:
    disabled: false
