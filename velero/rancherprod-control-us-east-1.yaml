#
# AWS rancher-prod us-east-1 (local)
#
configuration:
  features: "EnableAPIGroupVersions"
  extraEnvVars:
    AWS_CLUSTER_NAME: rancher-prod
  backupStorageLocation:
    bucket: account-233164232659-us-east-1-kaas-velerobackups
    prefix: rancher-prod
    config:
      region: us-east-1
  volumeSnapshotLocation:
    config:
      region: us-east-1
schedules:
  hourly:
    disabled: false
  daily:
    disabled: false
